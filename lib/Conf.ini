[Db]
DB_HOST="localhost"
DB_PORT=3306
DB_USER="www-data"
DB_PASS="www-data"
DB_NAME="kanshi"
DB_CHAR="utf8"

[Session]
SQL="SELECT COUNT(session) FROM tbl_session WHERE session = $_SESSION['session'] AND "

[Alert]
INSERT="INSERT INTO tbl_alert ( hostname, occurDate, alertLevel_id, alertContent ) VALUES ( :hostname, cast(now() as datetime), :alertLevel_id, :alertContent );"
SELECT="SELECT alert_id, hostname, occurDate, alertLevel, alertContent, checked FROM tbl_alert as a INNER JOIN tbl_alertLevel as b ON a.alertLevel_id = b.alertLevel_id ORDER BY occurDate DESC LIMIT :offset , :count ;" 
DELETE="DELETE FROM tbl_alert WHERE alert_id = :alert_id;"
UPDATE="UPDATE tbl_alert SET checked = :checked WHERE alert_id = :alert_id;"

[Host]
HOSTINSERT="INSERT INTO tbl_host ( hostname, ipaddress ) VALUES ( :hostname, :ipaddress );"
MONINSERT="INSERT INTO tbl_monitor ( monitorName, monitorType, host_id, argument, timeout ) VALUES ( :minitorName, :monitorType, :host_id, :argument, :timeout );"
HOSTSELECT="SELECT host_id, hostname, ipaddress FROM tbl_host ORDER BY hostname LIMIT :offset, :count;"
MONSELECT="SELECT monitor_id, monitorName, monitorType, host_id, b.hostname, argument, timeout FROM tbl_monitor as a INNER JOIN tbl_host as b ON a.host_id = b.host_id ORDER BY monitorName LIMIT :offset, :count;"
HOSTUPDATE="UPDATE tbl_host SET hostname=:hostname, ipaddress=:ipaddress WHERE host_id=:host_id;"
MONUPDATE="UPDATE tbl_monitor SET monitorName=:monitorName, monitorType=:monitorType, argument=:argument, timeout=:timeout WHERE monitor_id=:monitor_id;"
